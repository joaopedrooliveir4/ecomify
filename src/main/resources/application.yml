spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:h2:mem:ecomify
    driverClassName: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true

server:
  port: 8081
